services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'

  postgres-auth:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: novadesk
      POSTGRES_PASSWORD: novadesk
      POSTGRES_DB: auth_db
    ports:
      - '5432:5432'
