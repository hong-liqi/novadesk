# @portfolio/auth

Authentication utilities: JWT types, guards, decorators for NestJS and client auth foundation.

## Exports

- `JwtAuthGuard`, `RolesGuard`
- `@Public()`, `@Roles()`, `@CurrentUser()`
- `JwtPayload`, `AuthUser` types
