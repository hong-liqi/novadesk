/** Administration — route scaffold (no UI implementation) */
export default function AdministrationPage() {
  return (
    <section>
      <h1>Administration</h1>
      <p>Architecture scaffold — implementation pending.</p>
    </section>
  );
}
