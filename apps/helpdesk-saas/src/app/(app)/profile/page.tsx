/** Profile — route scaffold (no UI implementation) */
export default function ProfilePage() {
  return (
    <section>
      <h1>Profile</h1>
      <p>Architecture scaffold — implementation pending.</p>
    </section>
  );
}
