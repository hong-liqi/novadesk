/** Analytics — route scaffold (no UI implementation) */
export default function AnalyticsPage() {
  return (
    <section>
      <h1>Analytics</h1>
      <p>Architecture scaffold — implementation pending.</p>
    </section>
  );
}
