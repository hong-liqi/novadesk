/** Search — route scaffold (no UI implementation) */
export default function SearchPage() {
  return (
    <section>
      <h1>Search</h1>
      <p>Architecture scaffold — implementation pending.</p>
    </section>
  );
}
