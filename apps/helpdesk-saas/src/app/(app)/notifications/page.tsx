/** Notifications — route scaffold (no UI implementation) */
export default function NotificationsPage() {
  return (
    <section>
      <h1>Notifications</h1>
      <p>Architecture scaffold — implementation pending.</p>
    </section>
  );
}
