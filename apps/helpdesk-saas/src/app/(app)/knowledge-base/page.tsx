/** Knowledge Base — route scaffold (no UI implementation) */
export default function KnowledgeBasePage() {
  return (
    <section>
      <h1>Knowledge Base</h1>
      <p>Architecture scaffold — implementation pending.</p>
    </section>
  );
}
